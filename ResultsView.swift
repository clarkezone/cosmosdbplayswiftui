import SwiftUI

import SwiftUI
import CryptoKit
import Foundation

struct ResultsView: View {
    var body: some View {
        VStack {
Text("Results")
        }
        .navigationBarTitle("Results")
        .navigationBarTitleDisplayMode(.inline)
    }
}
